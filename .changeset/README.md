# Changesets

Run `pnpm changeset` for user-facing package changes.

V1 packages are linked so `@svg-editor/core`, `@svg-editor/adapter-utils`, `@svg-editor/react`, and `@svg-editor/vue` move together unless a future release plan splits them.
